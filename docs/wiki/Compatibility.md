# Compatibility

A changed Steam BuildID is detected automatically but never approved
automatically. Native hooks remain disabled for unknown builds until their
metadata, layouts, ABI assumptions, and runtime behavior have been reviewed.
